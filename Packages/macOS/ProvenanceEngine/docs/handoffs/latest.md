# Provenance Engine Package Handoff

Provenance Engine is now an independent Swift package inside the bmux monorepo
at `Packages/macOS/ProvenanceEngine`.

The current canonical handoff is the monorepo root file:

- `../../../../../docs/handoffs/latest.md`

The imported PE handoff history before the monorepo migration is preserved in
Git history and should be treated as historical. Do not use the old peer-repo
Project Truth commands from that history for current work.
