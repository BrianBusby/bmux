# PR #119 workbench acceptance checklist

Reference: `pr-119-swift-design-package.zip`, `approved-workbench-reference.png`.

Rendered evidence below distinguishes the supplied build-609 screenshot from the isolated native route `bmux DEV hybrid-focus-reference`, build 617. The live window target is 1000×700, dark appearance, global font magnification 110%; the rich rail and Focus/history fixtures are Debug-only and opt-in via `bmux.hybridFocus.fixture`. Sample card content is inert, but fixture card selection now delegates to the real workspace store; it does not send provider input or write ticket state. The supplied build-612 screenshot is Terminal and shows the obsolete perimeter before this correction.

| Requirement | Status | Evidence | Exception or remaining defect |
| --- | --- | --- | --- |
| R1 outer charcoal shell | partial | Supplied build-612 Terminal screenshot; build 614 source/build | The corrected build removes the obsolete perimeter and adds an opaque rail surface; final rendered after-capture is unavailable. |
| R2 workspace rail density | implemented—visually unverified | Build 612 DEBUG fixture path `HybridWorkbenchFixtureRail` in `Sources/ContentView.swift` | Three rich cards, retained purple selection, status/activity and scrollable resource rows are in the native sidebar composition. Current-build geometry remains uncaptured. |
| R3 rich workspace cards/resources | implemented—visually unverified | Build 617 DEBUG fixture cards in `Sources/ContentView.swift` plus production ticket-row rendering | Separate selection button and Link controls, multiple references, PR state/owner, project, long activity and honest missing references are implemented. Selected fixture resources hide while selected; inactive resources retain links; sample resource targets are fixture-only. |
| R4 stacked workspace header/chips | partial | Brian-supplied build-609 dark Chat screenshot | The live PR chip and stacked heading are visible; owner/project overflow fixture remains uncaptured, and the supplied image predates the latest source correction. |
| R5 intrinsic Focus/Chat/Terminal/Learnings nav | visually verified | Brian-supplied build-609 dark Chat screenshot | The four labels and underline navigation are visible in the normal shell; current build-612 after-correction capture remains unavailable. |
| R6 current-turn card | implemented—visually unverified | Build 617 DEBUG Focus fixture in `Sources/Panels/AgentSessionFactualProjectionView.swift`; live Focus implementation retained | Current objective, status, concise result, source-qualified checks and expandable tool evidence are represented in the same native Focus host. Fixture data is explicitly non-telemetry and does not prove live PE identity. |
| R7 quiet prior-turn history | implemented—visually unverified | Build 617 DEBUG Focus fixture in `Sources/Panels/AgentSessionFactualProjectionView.swift` | Three stable newest-first rows expand to Changed/Checked/Remains; live ordering/projection behavior remains covered by existing tests. Fixture is not live PE history. |
| R8 Learnings/footer honesty | partial | Live source path and reported unavailable state | Honest unavailable state is present; reference-style learning strip and restrained footer are not present. No current-build Learnings screenshot is available. |
| D1 header before navigation | visually verified | Brian-supplied build-609 dark Chat screenshot | Header precedes navigation in the supplied image; latest build remains uncaptured. |
| D2 stacked repo/title/chips | visually verified | Brian-supplied build-609 dark Chat screenshot | Repository, title and PR chip are visibly stacked; latest build remains uncaptured. |
| D3 label-width underline/order | visually verified | Brian-supplied build-609 dark Chat screenshot | Focus / Chat / Terminal / Learnings order and intrinsic underline are visible; latest build remains uncaptured. |
| D4 card-only selection border | implemented—visually unverified | Build 617 source path; shared connected perimeter and geometry preference were removed; selected card now owns a fine closed 1px border and restrained assigned-color fill | Supplied build-612 screenshot shows the obsolete orange perimeter; the correction is not yet rendered in a new capture. |
| D5 selected links move/header | partial | Build 617 source path; independent review of supplied build 609 screenshot identified the remaining selected subtitle duplication and it is corrected | Production cards suppress selected resource rows and the production header owns projected links. Fixture selection delegates to the live store, but fixture sample metadata is not injected into the real header, so fixture relocation is not proven. Final rendered after-capture is blocked. |
| D6 dark Focus composition | implemented—visually unverified | Build 617 DEBUG Focus fixture and shared Focus host | Populated current/history composition is reachable through the normal Focus tab in fixture mode; no rendered build-617 screenshot due capture failure. |
| live Terminal route | pass | CUA build 607 Terminal capture | Native terminal remains the active provider surface. |
| live Chat route | partial | Supplied build-609 dark Chat screenshot | Same shell/nav is shown; draft/identity retention is not proven by this image. |
| narrow/light themes | pending | Not yet captured in this checkpoint | Must capture after shell correction. |
| capture path | fail | CoreGraphics previously confirmed a tagged window on-screen at 1000×700 with sharing state 1; CUA exact app attach and ScreenCaptureKit alternative | CUA returns `Computer Use server error -10005: cgWindowNotFound`; `screencapture` cannot create an image because Screen Recording authorization is false, and the ScreenCaptureKit attempt aborts in `CGS_REQUIRE_INIT`. No system permissions were changed. |
