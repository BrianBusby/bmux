# Hybrid Focus visual checkpoint

Updated 2026-09-20 after reference comparison correction.

- Source: `hybrid-focus-design` after the follow-up visual/data commit.
- Built identity: tagged Debug build 614, `hybrid-focus-reference`; the isolated build used the existing local Swift package/Ghostty cache because package fetches were unavailable. The build succeeded, but CUA still fails to attach to the tagged app window.
- Slice 1: the normal native route retains live workspace store/selection/link behavior. Build 614 adds an opt-in Debug fixture rail through the sidebar composition with three rich cards, assigned colors, multiple references and independent Link controls; sample content is inert, while card selection delegates to the real workspace store. Selected fixture resources hide as they do on live cards, inactive links remain independent, and production ticket rows now render from the existing snapshot. The obsolete shared-coordinate perimeter is removed; selected cards now use a restrained assigned-color fill and fine closed local border. The rail has an opaque semantic surface beneath existing material.
- Slice 2: shared stacked workspace header, current-evidence-first Focus composition, restrained newest-first history, disclosure-backed source identity, coordinated light/dark shell tokens, and an opt-in populated Focus/history fixture are implemented through the real Swift shell components. The fixture is visual-only and does not prove live PE identity/order.
- Slice 3: native projection now forwards PR state, PR owner, ticket owner, project, and all known resource links; owner links are distinct from PR/ticket links and link activation does not select a workspace.
- Reference comparison correction: moved the header above navigation, changed navigation to Focus/Chat/Terminal/Learnings with intrinsic-width underlines, removed the standalone accent bar, stacked repo/title/resource chips, made the current turn a bordered card, and replaced the raw-ID-first latest-turn presentation with an objective/status/summary/progress card whose source IDs remain behind evidence disclosure.
- Capture diagnosis: moving the isolated tag's saved geometry to an on-screen primary-display frame previously resolved CUA for build 607. For build 612, exact app-path/name CUA attach, after a fresh CUA reset and a relaunch/wake attempt, returns `Computer Use server error -10005: cgWindowNotFound`; shell `screencapture` remains blocked by Screen Recording authorization, and a ScreenCaptureKit attempt aborts in `CGS_REQUIRE_INIT`. No global settings were changed.
- Visual evidence: the supplied archive's approved dark reference and current build-612 Terminal screenshot were inspected. The current screenshot shows the obsolete perimeter and wallpaper bleed; the source correction removes the perimeter, restores card-only selection, and adds an opaque rail surface. Rich rail and populated history fixtures are compiled through native Swift components, with selection now delegating to the real workspace store; build-614 post-correction rendered evidence is unavailable because capture cannot attach. Narrow/light, expanded-history and corrected card-only states remain visually unverified.
