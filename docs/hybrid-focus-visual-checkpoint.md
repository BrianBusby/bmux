# Hybrid Focus visual checkpoint

Updated 2026-09-20 after reference comparison correction.

- Source: `hybrid-focus-design` after the follow-up visual/data commit.
- Built identity: tagged Debug build 609, `hybrid-focus-reference`; the isolated build used the existing local Swift package/Ghostty cache because package fetches were unavailable. The app window is on-screen at 1000×700, but CUA failed to reattach after the latest relaunch.
- Slice 1: native workspace cards, assigned-color selection, 340px/290px rail policy, independent link activation, and underline view navigation are implemented in the normal app path. The shared-coordinate joined perimeter is now implemented and the active card no longer draws a closed local stroke; build 609 rendering is pending reattachment.
- Slice 2: shared stacked workspace header, current-evidence-first Focus composition, restrained newest-first history, disclosure-backed source identity, and coordinated light/dark shell tokens are implemented.
- Slice 3: native projection now forwards PR state, PR owner, ticket owner, project, and all known resource links; owner links are distinct from PR/ticket links and link activation does not select a workspace.
- Reference comparison correction: moved the header above navigation, changed navigation to Focus/Chat/Terminal/Learnings with intrinsic-width underlines, removed the standalone accent bar, stacked repo/title/resource chips, made the current turn a bordered card, and replaced the raw-ID-first latest-turn presentation with an objective/status/summary/progress card whose source IDs remain behind evidence disclosure.
- Capture diagnosis: moving the isolated tag's saved geometry to an on-screen primary-display frame previously resolved CUA for build 607. The build 609 window is currently on-screen, but CUA returns `cgWindowNotFound` after relaunch and shell `screencapture` remains blocked by Screen Recording authorization; no global settings were changed.
- Visual evidence: the supplied archive's approved dark reference and build-605 before-state were inspected. Independent review captured build 607 live Focus/Chat/Terminal/Learnings at 1000×700 dark/110% and found the rail/cards, joined outline, selected-link relocation, and rich history composition incomplete. Build 609 source is compiled but visually unverified. Rich multi-card fixture coverage, narrow layout and expanded history remain open.
