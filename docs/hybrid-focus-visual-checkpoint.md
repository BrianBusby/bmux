# Hybrid Focus visual checkpoint

Updated 2026-09-20 after reference comparison correction.

- Source: `hybrid-focus-design` after the follow-up visual/data commit.
- Built identity: tagged Debug build 610, `hybrid-focus-reference`; the isolated build used the existing local Swift package/Ghostty cache because package fetches were unavailable. CoreGraphics confirms PID 55403/window 11654 on-screen at 1000×700; CUA still fails to attach.
- Slice 1: the normal native route retains live workspace store/selection/link behavior. Build 610 adds an opt-in, inert Debug fixture rail through the sidebar composition with three rich cards, assigned colors, multiple references and independent Link controls; it does not replace live data. The shared-coordinate joined perimeter now uses the selected workspace color, removes the full-height sidebar divider, and the active card no longer draws a closed local stroke.
- Slice 2: shared stacked workspace header, current-evidence-first Focus composition, restrained newest-first history, disclosure-backed source identity, coordinated light/dark shell tokens, and an opt-in populated Focus/history fixture are implemented.
- Slice 3: native projection now forwards PR state, PR owner, ticket owner, project, and all known resource links; owner links are distinct from PR/ticket links and link activation does not select a workspace.
- Reference comparison correction: moved the header above navigation, changed navigation to Focus/Chat/Terminal/Learnings with intrinsic-width underlines, removed the standalone accent bar, stacked repo/title/resource chips, made the current turn a bordered card, and replaced the raw-ID-first latest-turn presentation with an objective/status/summary/progress card whose source IDs remain behind evidence disclosure.
- Capture diagnosis: moving the isolated tag's saved geometry to an on-screen primary-display frame previously resolved CUA for build 607. Build 610 is on-screen with a valid CoreGraphics window, but exact app-path/name CUA attach, after a fresh CUA reset, returns `Computer Use server error -10005: cgWindowNotFound`; shell `screencapture` remains blocked by Screen Recording authorization. No global settings were changed.
- Visual evidence: the supplied archive's approved dark reference, build-605 before-state, and Brian's build-609 dark Chat screenshot were inspected. Independent review found the build-609 selected prompt/URL duplication and continuous divider seam; source corrections are in build 610. Rich rail and populated history fixtures are compiled and reachable by opt-in defaults, but build-610 rendered evidence is unavailable because CUA cannot attach. Narrow/light, expanded-history and post-fix perimeter states remain visually unverified.
