# Hybrid Focus visual checkpoint

Updated 2026-09-20 after reference comparison correction.

- Source: `hybrid-focus-design` after the follow-up visual/data commit.
- Built identity: tagged Debug build 621, `hybrid-focus-reference`; the isolated build used the existing local Swift package/Ghostty cache because package fetches were unavailable. The build succeeded, but CUA still fails to attach to the tagged app window.
- Slice 1: the normal native route retains live workspace store/selection/link behavior. Build 614 adds an opt-in Debug fixture rail through the sidebar composition with three rich cards, assigned colors, multiple references and independent Link controls; sample content is inert, while card selection delegates to the real workspace store. Selected fixture resources hide as they do on live cards, inactive links remain independent, and production ticket rows now render from the existing snapshot. The obsolete shared-coordinate perimeter is removed; selected cards now use a restrained assigned-color fill and fine closed local border. The rail has an opaque semantic surface beneath existing material.
- Slice 2: shared stacked workspace header, current-evidence-first Focus composition, restrained newest-first history, disclosure-backed source identity, coordinated light/dark shell tokens, and an opt-in populated Focus/history fixture are implemented through the real Swift shell components. Fixture selection delegates to the live workspace store, but sample metadata is not injected into the real header and the fixture remains visual-only; it does not prove live PE identity/order.
- Slice 3: native projection now forwards PR state, PR owner, ticket owner, project, and all known resource links; owner links are distinct from PR/ticket links and link activation does not select a workspace.
- Reference comparison correction: moved the header above navigation, changed navigation to Focus/Chat/Terminal/Learnings with intrinsic-width underlines, removed the standalone accent bar, stacked repo/title/resource chips, made the current turn a bordered card, and replaced the raw-ID-first latest-turn presentation with an objective/status/summary/progress card whose source IDs remain behind evidence disclosure.
- Capture diagnosis: moving the isolated tag's saved geometry to an on-screen primary-display frame previously resolved CUA for build 607. For build 612, exact app-path/name CUA attach, after a fresh CUA reset and a relaunch/wake attempt, returns `Computer Use server error -10005: cgWindowNotFound`; shell `screencapture` remains blocked by Screen Recording authorization, and a ScreenCaptureKit attempt aborts in `CGS_REQUIRE_INIT`. No global settings were changed.
- Native shell ownership correction: the normal `TerminalPanelView` route owns the workspace header, Focus/Chat/Terminal/Learnings navigation, Focus/Terminal/Learnings content and Chat host. The React entry point now returns only `TerminalChatSurface` when the native bridge marks the context `readOnlyTerminalChat`; it no longer mounts a second rail/header/nav in Chat. The native workspace header is full-width and leading-aligned, and the stale React 3px purple perimeter/closed edge was removed.
- Independent review: the normal native route passes source-level ownership and alignment review for all four tabs on build 620/621. A separate legacy `AgentSessionPanelView` still mounts the standalone web workbench; it is not the selected native workspace route and remains a separately scoped caution.
- Visual evidence: the supplied approved dark reference and Brian's build-617 Chat/Focus screenshots were inspected. They show the duplicate Chat workbench, inset header and purple perimeter that motivated this correction; they are before-state evidence. Build-621 contains the native-only Chat mount, full-width header, neutral closed React card border and labeled live Focus Results/Current activity presentation. Narrow/light, expanded-history and corrected build-621 rendering remain visually unverified.
