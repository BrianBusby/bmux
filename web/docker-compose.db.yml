services:
  postgres:
    image: postgres:16-alpine
    container_name: ${BMUX_DB_CONTAINER_NAME}
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${BMUX_DB_NAME}
      POSTGRES_PASSWORD: ${BMUX_DB_PASSWORD}
      POSTGRES_USER: ${BMUX_DB_USER}
    ports:
      - "${BMUX_DB_PORT}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${BMUX_DB_USER} -d ${BMUX_DB_NAME}"]
      interval: 1s
      timeout: 5s
      retries: 30
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data:
    name: ${BMUX_DB_VOLUME_NAME}
