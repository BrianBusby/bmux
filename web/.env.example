RESEND_API_KEY=
BMUX_FEEDBACK_FROM_EMAIL=
BMUX_FEEDBACK_RATE_LIMIT_ID=
BMUX_CLIENT_CONFIG_RATE_LIMIT_ID=

# bmux Founder's Edition welcome email. /api/stripe/founders-welcome verifies the
# Stripe webhook signature with this endpoint secret, then sends the welcome email
# via Resend (To customer, CC the founders). Leave the secret empty to disable the
# route. BMUX_FOUNDERS_FROM_EMAIL overrides the sender (defaults to
# austin@manaflow.ai) and must be on a Resend-verified domain.
BMUX_FOUNDERS_FROM_EMAIL=
STRIPE_FOUNDERS_WEBHOOK_SECRET=

# Direct Stripe billing for bmux Pro. Leave unset to use the legacy Stack-hosted
# checkout flow. Price ids are optional overrides; otherwise the app resolves
# prices by lookup key in Stripe test/live mode.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRO_MONTHLY_PRICE_ID=
STRIPE_PRO_YEARLY_PRICE_ID=
STRIPE_TEAM_MONTHLY_PRICE_ID=

# App Store Connect API for iOS TestFlight enrollment. Leave empty to show the
# dashboard action as unavailable. ASC_PRIVATE_KEY accepts PEM contents with
# literal "\n" escapes; ASC_PRIVATE_KEY_PATH is a local-dev fallback.
ASC_KEY_ID=
ASC_ISSUER_ID=
ASC_PRIVATE_KEY=
ASC_PRIVATE_KEY_PATH=
BMUX_TESTFLIGHT_APP_ID=6757092429
BMUX_TESTFLIGHT_GROUP_ID=3ee84bfa-10ad-4f23-a45c-f9a3b037373e

# Server-side error reporting. Optional; no-op when unset.
SENTRY_DSN=

# Stack Auth (https://stack-auth.com), required for the /handler/* routes
# and the mac app sign-in flow.
NEXT_PUBLIC_STACK_PROJECT_ID=
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=
STACK_SECRET_SERVER_KEY=

# Local/Postgres database. `bun db:*` derives the local port from BMUX_PORT by default:
# DATABASE_URL uses localhost:${BMUX_PORT + 10000}. CI should set explicit URLs.
DATABASE_URL=
DIRECT_DATABASE_URL=

# Cloud VM providers (manaflow-owned keys, no BYO). See scratch/vm-experiments for images.
# Global create kill switch. Set to 0/false/off to block new paid provider creates while keeping
# list, attach, and delete endpoints available.
BMUX_VM_CREATE_ENABLED=1
# Which provider a fresh `bmux vm new` picks if the client doesn't specify one.
BMUX_VM_DEFAULT_PROVIDER=e2b
# Dev-only escape hatch for image experiments. Leave unset in Vercel production/staging/preview so
# image ids must be present in services/vms/images/manifest.json.
BMUX_VM_ALLOW_UNMANIFESTED_IMAGES=
# Optional comma-separated same-origin exceptions for cookie-authenticated browser mutations.
BMUX_VM_ALLOWED_ORIGINS=
BMUX_VM_E2B_ENABLED=1
E2B_API_KEY=
# E2B bmuxd WebSocket PTY template. Produced by web/scripts/build-cloud-vm-images.ts.
E2B_BMUXD_WS_TEMPLATE=bmuxd-ws:proxy-20260424a
# Legacy scratch template. Pass explicitly with --image if needed.
E2B_SANDBOX_TEMPLATE=bmux-sandbox:v0-71a954b8e53b
BMUX_VM_FREESTYLE_ENABLED=0
FREESTYLE_API_KEY=
FREESTYLE_SANDBOX_SNAPSHOT=sc-mt237w1nd7c7673bd03m

# Cloud VM database. Local dev derives DATABASE_URL from BMUX_PORT. Staging/production use the
# Vercel Marketplace AWS Aurora PostgreSQL OIDC/RDS IAM env set below.
BMUX_DB_DRIVER=
BMUX_DB_POOL_MAX=5
# Verify AWS RDS server certificates. Leave unset for the current Vercel Marketplace Aurora
# databases so Node uses its default trust store.
BMUX_DB_SSL_REJECT_UNAUTHORIZED=
AWS_ROLE_ARN=
AWS_REGION=
PGDATABASE=
PGHOST=
PGPORT=
PGUSER=

# Optional Stack Auth create-credit gating. Leave this empty for active-limit-only
# free-plan enforcement. Set an item id only when the free plan should consume
# one team-scoped create-credit item per successful create.
BMUX_VM_PLAN_FREE_CREATE_CREDIT_ITEM_ID=
BMUX_VM_PLAN_FREE_CREATE_CREDIT_COST=
# Optional global or provider-specific overrides. Paid plans use create credits only when
# a plan-specific or global item id is configured.
BMUX_VM_CREATE_CREDIT_COST=
BMUX_VM_CREATE_CREDIT_COST_E2B=
BMUX_VM_CREATE_CREDIT_COST_FREESTYLE=
BMUX_VM_CREATE_CREDIT_ITEM_ID=

# bmux Vault cloud sync. Leave BMUX_VAULT_S3_BUCKET empty to disable the upload,
# commit, and download routes. BMUX_VAULT_S3_ENDPOINT is for S3-compatible
# storage such as Cloudflare R2. When access keys are omitted, the AWS default
# provider chain is used.
BMUX_VAULT_ENABLED=
BMUX_VAULT_S3_BUCKET=
BMUX_VAULT_S3_REGION=auto
BMUX_VAULT_S3_ENDPOINT=
BMUX_VAULT_S3_ACCESS_KEY_ID=
BMUX_VAULT_S3_SECRET_ACCESS_KEY=
BMUX_VAULT_PRESIGN_TTL_SECONDS=900
BMUX_VAULT_MAX_UPLOAD_BYTES=536870912
# Per-user cap on total committed compressed Vault bytes. Defaults to 50 GiB.
BMUX_VAULT_MAX_USER_BYTES=53687091200

# OpenTelemetry/Axiom.
OTEL_EXPORTER_OTLP_ENDPOINT=
OTEL_EXPORTER_OTLP_HEADERS=
OTEL_SERVICE_NAME=
