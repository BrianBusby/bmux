interface:
  display_name: "bmux Architecture"
  short_description: "Apply bmux package, refactor, file organization, DocC, testability, and concurrency rules."
  default_prompt: "Use this skill before adding or meaningfully rewriting Swift files, Swift packages, coordinators, services, repositories, package APIs, or Swift concurrency code in bmux."
