interface:
  display_name: "bmux Workspace"
  short_description: "Scope bmux automation to the current workspace, caller terminal, panes, and surfaces."
  default_prompt: "Use this skill to identify the current bmux caller workspace and surface, then keep workspace, pane, surface, browser, sidebar, and socket actions scoped to that context unless the user explicitly names another target."
