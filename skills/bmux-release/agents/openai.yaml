interface:
  display_name: "bmux Release"
  short_description: "Prepare bmux releases with version, changelog, guard, tag, and asset rules."
  default_prompt: "Use this skill when preparing a bmux release, bumping versions, updating CHANGELOG.md, running the release pretag guard, tagging, or troubleshooting release assets."
