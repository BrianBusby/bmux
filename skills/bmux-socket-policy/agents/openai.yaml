interface:
  display_name: "bmux Socket Policy"
  short_description: "Keep bmux socket commands off hot main-thread paths and focus-safe."
  default_prompt: "Use this skill when adding or changing bmux socket or CLI commands, telemetry handlers, focus/select/open/close/send-key behavior, or automation that could activate the app or steal focus."
