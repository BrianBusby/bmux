interface:
  display_name: "bmux Backend"
  short_description: "Use Effect and Postgres-backed Cloud VM rules for backend work."
  default_prompt: "Use this skill when editing bmux backend TypeScript, Vercel route handlers, Effect services, Cloud VM lifecycle code, provider integrations, database migrations, or pricing gates."
