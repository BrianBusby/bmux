interface:
  display_name: "bmux Dev Workflow"
  short_description: "Follow bmux setup, Xcode, and tagged ExtensionKit development rules."
  default_prompt: "Use this skill when setting up bmux, touching Xcode project files, working with tagged debug builds, or adding sidebar extensions that must coexist with other dev builds."
