interface:
  display_name: "bmux Shared Behavior"
  short_description: "Keep multi-entrypoint behavior and optimistic updates on one shared path."
  default_prompt: "Use this skill when changing behavior exposed through multiple bmux entrypoints, optimistic UI or CLI mutation flows, or tests that previously missed a user-visible bug."
